version: "3.9"
services:
  nginx:
    container_name: nginx
    build:
      context: ./proxy/
      dockerfile: Dockerfile
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - './backend:/app/'
      - './proxy/nginx.conf:/etc/nginx/nginx.conf'
      - './proxy/nginx-selfsigned.crt:/etc/nginx/ssl/nginx-selfsigned.crt'
      - './proxy/nginx-selfsigned.key:/etc/nginx/ssl/nginx-selfsigned.key'
      - './certbot/www/:/var/www/certbot/:ronslookup'
    depends_on:
      - backend
      - frontend
    networks:
      - funny
  db:
    image: postgres:16
    container_name: db
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_HOST==${DB_HOST}
      - POSTGRES_DB=${DB_DATABASE}
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    networks:
      - funny


networks:
  funny:
    driver: bridge